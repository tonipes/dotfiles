#!/bin/bash

REMOTE_PATH=/mnt/knuxraid
LOCAL_PATH=/mnt/knuxraid
PORT=1022
#if [ "$1" -gt "-u" ] ; then 
#	umount $MOUNT_PATH
#fi

sshfs -o reconnect -o ServerAliveInterval=15 -o ServerAliveCountMax=3 toni@tonipes.net:$REMOTE_PATH $LOCAL_PATH -p $PORT
